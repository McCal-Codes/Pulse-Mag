# pnpm store stays on C: (NTFS) — I: drive is exFAT, no symlink support
# store-dir is intentionally left to pnpm's default (C:\Users\...\AppData\Local\pnpm\store)

# Use hoisted node_modules (no symlinks) for exFAT compatibility
node-linker=hoisted
